import MapViewPort from '../components/MapViewport';
import {toScreenCoordinate, toWorldCoordinate} from '../utils/coordinate';

export default function Aino() {

    return (
        <MapViewPort/>
    )
}